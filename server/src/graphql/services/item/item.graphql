extend type Query {
  itemByID(id: String!): Item!
  itemByName(name: String!): [Item!]!
}

type Item {
  id: String!
  name: String!
  quantity: Int!
  costPerUnit: Float!
  pricePerUnit: Float!
  discount: Float!
}