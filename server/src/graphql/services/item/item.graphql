extend type Query {
  items(organizationId: ID!, branchId: ID!): [Item!]!
  item(organizationId: ID!, branchId: ID!, itemId: ID!): Item
}

type Item {
  id: String!
  name: String!
  quantity: Int!
  costPerUnit: Float!
  pricePerUnit: Float!
  discount: Float!
}