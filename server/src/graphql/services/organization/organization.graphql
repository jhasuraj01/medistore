extend type Query {
  organization(id: ID!): Organization
  shop(id: ID!): Shop
  organizations: [Organization!]!
  shops: [Shop!]!
}
type Organization {
  id: ID!
  name: String!
  shops: [Shop!]!
}
type Shop {
  id: ID!
  name: String!
  organization: Organization!
}